version: '3.9'

services: 
  server:
    build: .
    ports: 
      - '5000:5000'
  db:
    image: 'postgres'
    environment: 
      POSTGRES_PASSWORD: 'admin1'
      POSTGRES_USER: 'docker'

      npx create-react-app myfrontend